# Contributing

1. Open an issue or discuss the change first for large features.
2. Keep file I/O behind `server/storage` and `file-service`.
3. Add tests for path, permission, and token changes.
4. Run `npm test` and `npm run typecheck`.
5. Do not commit `.env` or `/storage`.

# Extra Host-Mounts (manuell). UI-Volumes liegen in docker-compose.cloudora-volumes.yml.
#
# services:
#   cloudora:
#     volumes:
#       - /mnt/nas/photos:/storage/volumes/photos
#       - /home:/home
#
# Danach: docker compose up -d --no-build

services: {}

# Extra Host-Mounts unter den Storage-Root.
# Diese Datei nach docker-compose.override.yml kopieren und volumes eintragen:
#
# services:
#   cloudora:
#     volumes:
#       - /mnt/hdd/cloudora:/storage
#       - /mnt/nas/cloudora:/storage
#       - /mnt/usb/photos:/storage/shared/photos
#       - /home:/home
#
# Home-Pfade in der Admin-UI z. B. /home oder /home/anna.
# Danach: docker compose up -d

services: {}
